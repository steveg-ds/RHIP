version: '3.8'
services:
  jupyterhub:
    image: jupyterhub/jupyterhub
    ports:
      - "8000:8000"
    volumes:
      - .:/home/jovyan/work
    command: jupyterhub --no-ssl --Spawner.default_url=/lab
